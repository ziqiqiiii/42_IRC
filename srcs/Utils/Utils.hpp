#pragma once

# include "ircserv.hpp"

namespace IRC
{
    namespace Utils
    {
        void    checker(int argc);
        string	stringToUpper(const string& str);
        string	intToString(int num);
    }
}
